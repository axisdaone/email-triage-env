name: email-triage-env
version: "1.0.0"
description: "An environment where an AI agent triages a realistic inbox of emails."
author: "axisdaone"
tags: ["email", "triage", "nlp", "productivity"]
license: "mit"
homepage: "https://huggingface.co/spaces/axisdaone/email-triage-env"
entrypoint: "main:app"
framework: "fastapi"

endpoints:
  reset: POST /reset
  step: POST /step
  state: GET /state