[project]
name = "email-triage-env"
version = "1.0.0"
description = "An OpenEnv environment for email triage"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "requests",
]